# Youbestar Agent 开发交接文档

## 项目定位

这是一个本地可控的 AI Agent 原型系统。

当前目标不是做复杂平台，而是先完成最小闭环：

- 网页对话
- 模型配置
- 第三方 API 模型列表发现
- 闲聊模式 / 任务优先模式
- LangGraph 旁路实验模式
- Agent Action 解析
- 技能调用
- 技能审批
- 技能注册表
- GitHub 同步上传

后续可以继续扩展 Excel、OCR、ERP、浏览器自动化和社区技能安装系统。

## 当前运行状态

后端：

```text
FastAPI + uvicorn
端口：8000
入口：server.py
```

前端：

```text
index.html
通过 FastAPI 根路径返回，也可以直接打开
```

启动：

```bat
start.bat
```

服务地址：

```text
http://127.0.0.1:8000/
```

## 主要模块说明

### `server.py`

负责：

- 返回 `index.html`
- 读写模型配置
- 接收聊天请求
- 调用 Agent loop
- 挂载技能系统 API

主要接口：

```text
GET  /health
GET  /
GET  /config
POST /config
POST /models/discover
POST /chat
POST /langgraph/chat
```

`POST /chat` 请求体支持：

```json
{
  "message": "你好",
  "allowChat": true,
  "history": []
}
```

`allowChat=True`：允许自然回复，模型可以输出 `Response:`。

`allowChat=False`：任务优先，只允许 `Thought / Action / Params`，后端会丢弃自然回复。

### 模型列表发现

配置页填写 API 地址和 API Key 后，可以调用：

```text
POST /models/discover
```

请求：

```json
{
  "api_url": "https://integrate.api.nvidia.com/v1",
  "api_key": "..."
}
```

后端会把兼容地址转换为 `/models`，携带 Bearer Key 请求第三方接口，并统一提取模型 ID。响应不会返回 API Key：

```json
{
  "models_url": "https://integrate.api.nvidia.com/v1/models",
  "models": ["model-a", "model-b"]
}
```

支持 OpenAI-compatible 的 `data: [{"id": "..."}]` 和常见 `models` 列表格式。不支持模型发现的第三方接口仍可手动填写模型名。

当用户只填写供应商根地址，且根 `/models` 返回 `404` 时，后端只会再尝试一次 `/v1/models`。成功后响应中的 `api_url` 会返回实际 `/v1` 基地址，前端同步更新并自动保存，避免聊天请求继续使用错误路径。

`POST /langgraph/chat` 是独立实验入口。请求额外传入：

```json
{
  "message": "打开百度",
  "allowChat": false,
  "threadId": "conversation-id"
}
```

响应会额外返回：

```json
{
  "engine": "langgraph",
  "thread_id": "conversation-id",
  "graph_nodes": ["plan", "execute_skill", "reflect", "finish"],
  "turn_count": 1
}
```

### `core/langgraph_bridge.py`

最小 LangGraph 旁路桥接。

状态图流程：

```text
plan -> no_action / execute_skill -> reflect -> finish
```

它复用现有 Prompt、解析器、技能审批状态和技能开关。图使用 `InMemorySaver` 按 `threadId` 隔离对话历史与轮次状态，仅用于能力测试；服务重启后状态会清空。
`reflect` 节点会在技能执行或无操作后做规则复盘：保留已有自然回复、把成功工具结果桥接为回复、把失败工具结果转成自然解释、为空的闲聊回复提供兜底。

### `core/llm.py`

负责请求 OpenAI-compatible 云端模型。

API 地址从 `youbestar.json` 读取。

大部分云端模型只要是 `/v1/chat/completions` 兼容格式，都可以接入。

### `core/loop.py`

Agent 主循环。

核心逻辑：

1. 构造 prompt
2. 要求模型输出 `Thought / Action / Params`
3. 根据 `allowChat` 决定是否允许 `Response`
4. 解析模型输出
5. 判断 Action 是否注册、是否开启
6. 调用技能
7. 返回模型输出、工具结果和可选用户回复

当前 prompt 已要求模型使用命名空间 Action：

```text
official.open_browser
local.xxx
community.author.xxx
```

### `core/parser.py`

负责解析模型输出：

```text
Thought: ...
Action: official.open_browser
Params: {"url": "https://www.baidu.com"}
Response: 你好
```

返回：

- `thought`
- `action`
- `params`
- `response`

`Response` 是可选字段，只在 Chat On 场景用于自然回复。

### `agent_system/manager.py`

技能系统核心。

负责：

- 创建目录
- 校验命名空间
- 写入 sandbox
- 写入测试
- 运行测试
- 提交审批
- 批准/拒绝
- 注册技能
- 加载并运行技能

关键设计：

```text
skills/
  official/
  community/
  local/
  registry.json
```

### `agent_system/skill_registry.py`

负责：

- 技能开关
- 技能卡片列表
- 兼容旧 Action 名称

例如旧名称：

```text
open_browser
```

会映射为：

```text
official.open_browser
```

### `agent_system/server.py`

技能系统的 FastAPI router。

主要接口：

```text
GET  /skills/prompt
GET  /skills/read-policy
POST /skills/files/list
POST /skills/files/read
GET  /skills/approved
GET  /skills/registry
POST /skills/toggle
GET  /skills/approvals
POST /skills/sandbox/write
POST /skills/tests/write
POST /skills/tests/run
POST /skills/approval/request
POST /skills/approve
POST /skills/reject
```

## 技能命名规则

必须使用命名空间：

```text
official.skill_name
community.author.skill_name
local.skill_name
```

含义：

- `official`：官方维护技能，可随项目版本升级
- `community`：社区共享技能，可下载、导入、升级
- `local`：本地用户技能，不自动更新

模型新写技能时，只允许进入：

```text
local.skill_name
```

## 技能注册表

文件：

```text
agent_system/skills/registry.json
```

示例：

```json
{
  "official.open_browser": {
    "path": "agent_system/skills/official/open_browser.py",
    "version": "1.0.0",
    "source": "official",
    "title": "打开浏览器",
    "description": "根据 URL 打开系统默认浏览器。",
    "author": "youbestar"
  }
}
```

运行逻辑：

```text
Action -> registry.json -> skill path -> import module -> run(params)
```

## 安全边界

模型允许：

- 写入 `agent_system/sandbox/`
- 写入测试
- 运行 sandbox 测试
- 提交审批
- 读取白名单内普通项目文件

模型禁止：

- 直接修改 `skills/`
- 删除文件
- 操作系统文件
- 读取密钥、token、cookie、auth 文件
- 调用 ERP、微信、发布、删除等高风险动作
- 执行未批准代码

安全扫描会拦截部分高风险 import 和函数调用。

## GitHub 交接

GitHub 仓库：

```text
https://github.com/bori830421-pixel/youbestar
```

两个 BAT 是相互独立的单向操作：

- `github_upload.bat`：以本地版本为准，上传并覆盖 GitHub。
- `github_sync.bat`：以 GitHub 为来源，只下载同步。

上传脚本会：

- 初始化 `.git`
- 设置远端 origin
- 设置分支为 `main`
- 检查 `youbestar.json` 是否被跟踪
- add / commit
- 刷新远端分支元数据，但不会 pull
- 使用 `git push --force-with-lease` 安全覆盖远端

下载同步脚本会：

- 初始化 `.git`
- 设置远端 origin
- 检查未提交的本地修改；发现修改立即停止并保护文件
- 检查远端 main 是否存在
- fetch
- 使用 `pull --ff-only` 下载；历史分叉时停止，不自动改写历史
- 永远不会 push

空仓库第一次没有远端 `main` 分支，需要先运行上传脚本。

## 当前测试

测试目录：

```text
tests/
```

运行：

```bat
.venv\Scripts\python.exe -m unittest discover -s tests
```

当前测试项：

- parser 解析 `Thought / Action / Params`
- skill namespace 规范化
- registry 注册和执行
- Agent loop 处理未注册 Action
- 第三方模型接口错误声明字符集时，仍按 UTF-8 正确解析中文 JSON
- 前端对话列表刷新/重开页面后可从浏览器本地存储恢复
- official.query_weather 天气技能注册、调用和结果格式化
- official.install_local_skill 可直接安装/覆盖 local.* 技能
- 工具执行结果可桥接为用户自然回复
- 自然闲聊默认只展示用户可见最终回复，调试过程默认隐藏
- LangGraph reflect 节点会在执行后复盘结果
- Youbestar 自研 Agent Runtime 骨架已建立
- official.write_project_file 可写入项目内普通文件
- core/ui_formatter.py 统一 Agent 用户可见输出格式

## 2026-06-04 中文乱码修复

- 根因：部分 OpenAI 兼容中转接口返回 UTF-8 JSON，但响应头没有正确声明字符集，`requests.Response.json()` 会产生中文乱码。
- 修复：`core/llm.py` 直接从响应原始字节按 `utf-8-sig` 解析 JSON。
- 回归测试：`tests/test_llm.py`
- 当前完整测试结果：24 项通过。

## 2026-06-04 对话记录持久化修复

- 根因：前端 `conversations` 只保存在页面内存中，关闭网页或刷新后会丢失。
- 修复：`index.html` 使用 `localStorage` 保存对话列表、当前选中对话和消息历史；重新进入应用时自动恢复。
- 细节：保存时过滤 `pending` 临时消息，避免把“正在思考...”状态持久化。
- 附带修复：`agent_system/manager.py` 的 `to_relative()` 兼容 Windows 临时目录长路径/短路径名不一致，避免完整测试在注册技能用例中误报。
- 回归测试：`tests/test_conversation_persistence_ui.py`
- 当前完整测试结果：26 项通过。

## 2026-06-04 天气查询技能

- 新增官方技能：`official.query_weather`
- 技能入口：`agent_system/skills/official/query_weather.py`
- 工具实现：`tools/weather_tool.py`
- 数据来源：Open-Meteo API，内置常用中国城市坐标，默认城市为汕头。
- 调用参数：`{"city": "汕头", "days": 7}`，`days` 限制为 1-7。
- Agent prompt 已增加规则：用户查询天气、天气预报、气温、下雨情况时调用 `official.query_weather`。
- 回归测试：`tests/test_weather_tool.py`
- 当前完整测试结果：29 项通过。

## 2026-06-04 自主安装本地技能模式

- 新增官方技能：`official.install_local_skill`
- 技能入口：`agent_system/skills/official/install_local_skill.py`
- 工具实现：`tools/evolution_tool.py` 调用 `agent_system.manager.install_local_skill()`
- 能力：Agent 可直接写入或覆盖 `agent_system/skills/local/` 下的 `local.*` 技能，并写入 `agent_system/skills/registry.json` 注册为正式技能。
- Prompt 已改为：用户要求创建或改进技能时，优先调用 `official.install_local_skill`，不再要求先写 sandbox、测试、提交人工审批。
- 边界：自主安装只允许 `local.*` 命名空间，路径仍限制在 `skills/local/`。
- 本地技能运行时不再套用旧的安全扫描；community 技能仍保留扫描。
- 回归测试：`tests/test_skill_registry.py`、`tests/test_agent_loop.py`
- 当前完整测试结果：31 项通过。

## 2026-06-04 工具结果回复桥接修复

- 根因：Agent 执行技能后只返回 `action_result`，如果模型没有提前写 `Response`，用户回复区域不会自然展示工具结果。
- 修复：新增 `core.loop.bridge_tool_result_to_response()`，在 `allowChat=True` 且存在工具 Action 时，把工具执行结果桥接为 `response`。
- 覆盖范围：普通 `/chat` Agent loop 和 `core/langgraph_bridge.py` LangGraph 旁路都已接入。
- 效果：查询天气这类技能调用会直接在用户回复区域返回天气结果，不再只显示在工具卡片里。
- 回归测试：`tests/test_agent_loop.py`、`tests/test_langgraph_bridge.py`
- 当前完整测试结果：33 项通过。

## 2026-06-04 自然聊天展示修复

- 根因：后端 `reply` 和前端主气泡都偏调试日志风格，会把 `模型输出 / 工具执行结果 / Thought / Action / Params` 暴露给用户，闲聊显得冷冰冰。
- 修复：`server.py` 的 `reply` 改为用户可见最终答案；调试信息继续保留在 `model_reply`、`thought`、`action`、`params`、`action_result` 字段。
- 前端：`index.html` 默认关闭思考和工具过程展示，主气泡优先显示 `response` / `reply` / `content`，调试按钮打开后仍可查看 Thought、Action、Params、Result 和 LangGraph 节点。
- Prompt：`core/loop.py` 明确要求 Response 是直接给用户看的自然、简洁、温暖回复，不要提内部协议字段。
- 回归测试：`tests/test_chat_reply.py`、`tests/test_natural_chat_ui.py`、`tests/test_agent_loop.py`
- 当前完整测试结果：40 项通过。

## 2026-06-04 LangGraph 复盘节点

- 根因：原 LangGraph 旁路仍是单次计划、单次执行、直接结束，缺少执行后的观察和复盘。
- 修复：`core/langgraph_bridge.py` 增加 `reflect` 节点，流程升级为 `plan -> no_action / execute_skill -> reflect -> finish`。
- 复盘规则：已有自然回复则保留；工具成功但没有回复时把结果桥接为回复；工具失败时把 `未知工具 / 技能已关闭 / 失败 / 错误 / error` 转成自然解释；无工具且无回复时给出温和兜底。
- 效果：`/langgraph/chat` 比普通 `/chat` 多一层“执行 -> 观察 -> 复盘 -> 回答”的思考结构。
- 回归测试：`tests/test_langgraph_bridge.py`

## 2026-06-04 Youbestar 自研 Agent Runtime 骨架

- 战略定位：Youbestar 最终要实现自己的独立智能体内核，LangGraph 只作为参考教材和实验对照，不作为正式核心依赖。
- 架构文档：`docs/architecture/youbestar-agent-runtime.md`
- 新增状态层：`core/agent_state.py`
- 新增节点层：`core/agent_nodes.py`
- 新增运行时：`core/agent_runtime.py`
- 新增检查点接口：`core/agent_checkpoint.py`
- 当前最小自研流程：`prepare -> execute -> reflect -> finalize`
- 行为：复用现有 prompt、parser、技能注册表、技能开关和工具执行；执行后通过 reflect 节点复盘结果；finalize 输出兼容现有 chat 响应字段的 `AgentResult`。
- `/chat` 已默认切换到自研 `AgentRuntime`；旧 `agent_loop` 仍作为兼容 fallback 保留。
- 回归测试：`tests/test_agent_runtime.py`
- 当前完整测试结果：45 项通过。

## 2026-06-04 `/chat` 接入自研 Agent Runtime

- 入口：`server.py`
- 默认行为：`/chat` 使用 `AgentRuntime().run(...)`，执行 `prepare -> execute -> reflect -> finalize`。
- 兼容退路：`run_legacy_agent_loop()` 保留旧 `agent_loop` 包装；`USE_AGENT_RUNTIME` 控制默认路径。
- 线程支持：`ChatRequest` 新增 `threadId` 字段，前端传入的对话 ID 可进入自研 Runtime。
- API 兼容：返回字段继续保持 `reply / model_reply / thought / action / params / action_result / response`。
- 回归测试：`tests/test_server_chat_runtime.py`
- 当前完整测试结果：49 项通过。

## 2026-06-04 项目文件写入技能

- 新增官方技能：`official.write_project_file`
- 技能入口：`agent_system/skills/official/write_project_file.py`
- 工具实现：`tools/file_access_tool.py` 调用 `agent_system.file_access.write_allowed_file()`
- 能力：允许 Agent 写入运行项目白名单目录内的普通文本或代码文件。
- 参数：`{"path": "docs/notes.md", "content": "...", "overwrite": false}`
- 安全边界：仍禁止写入 `.git`、`.venv`、`youbestar.json`、`.env`、token、cookie、credential、secret 等敏感路径或敏感命名文件。
- Prompt 已增加规则：用户要求直接在运行目录、项目目录或指定普通文件中写入/修改内容时，调用 `official.write_project_file`。
- 回归测试：`tests/test_project_file_write.py`、`tests/test_agent_loop.py`
- 当前完整测试结果：54 项通过。

## 2026-06-05 结构化输出格式层

- 根因：工具结果虽然已经执行成功，但主回复可能只显示模型的空泛说明，例如“正在为你查询...”，真实结果需要展开工具卡片才看得到。
- 新增模块：`core/ui_formatter.py`
- 提供函数：`format_order_result(data)`、`format_inventory(data)`、`format_error(msg)`、`format_agent_reply(action, response, action_result)`
- 输出规范：必须有一级标题；复杂内容拆二级模块；重点加粗；多条数据用 Markdown 表格；emoji 控制在标题和少量提示中。
- 接入位置：自研 `AgentRuntime`、`core/langgraph_bridge.py`、`server.py` 最终用户回复构造。
- 天气结果：`official.query_weather` 的多日结果会在最终回复中转成表格，不再只藏在工具调用卡片里。
- 回归测试：`tests/test_ui_formatter.py`、`tests/test_agent_runtime.py`、`tests/test_langgraph_bridge.py`、`tests/test_chat_reply.py`
- 当前完整测试结果：59 项通过。

## 2026-06-05 项目规则固化：技能输出与联网层

- 新增根目录规则文件：`AGENTS.md`
- 固化原则 1：技能只负责业务执行并返回结构化数据，不负责最终 Markdown 渲染；最终用户可见回复统一走 `core/ui_formatter.py`。
- 固化原则 2：联网技能不各自处理编码、请求头、超时和 JSON 解析；后续应新增或复用 `core/http_client.py`，统一提供 `fetch_text()`、`fetch_json()`。
- 编码策略：共享网络层负责从响应头识别 charset，并自动尝试 `utf-8-sig`、`utf-8`、`gb18030`、`gbk`，避免每个技能手写“某接口用 GBK”。
- 固化原则 3：技能按能力域归类，不无限平铺增长。建议域包括 `query_hub`、`market_data`、`weather_data`、`web_search`、`browser_headless`、`browser_desktop`。
- 浏览器能力拆分：桌面可见浏览器用于打开页面、登录、人工查看；无头浏览器用于后台搜索、抓取、提取和校验。
- 本次为文档/规则固化，未改业务代码。

## 2026-06-05 联网层与结构化技能结果改造

- 新增共享网络层：`core/http_client.py`
- 提供能力：`fetch_text()`、`fetch_json()`、`decode_bytes()`，统一处理请求头、超时、响应头 charset、`utf-8-sig` / `utf-8` / `gb18030` / `gbk` fallback 和 JSON 解析。
- `tools/weather_tool.py` 已改为使用 `fetch_json()`。
- `agent_system/skills/local/query_market_data.py` 已改为使用 `fetch_text()` / `fetch_json()`，腾讯行情 GBK 中文名称可正确解码。
- 证券技能不再返回手写 Markdown，而是返回结构化对象：`ok / kind / title / columns / rows / summary / data`。
- `core/ui_formatter.py` 新增 `format_skill_result(result)` 和 `observation_to_text(value)`，可统一渲染结构化技能结果，避免主回复显示 Python dict。
- 自研 `AgentRuntime` 的 `execute` 节点不再 `str(run_approved_skill(...))`，保留原始结构化结果；`finalize` 再统一格式化。
- `core/langgraph_bridge.py` 同步保留结构化工具结果，实验路径不再复制旧问题。
- `core/loop.py` prompt 已补充规则：Agent 自己创建联网技能时必须复用 `core.http_client`；技能只返回结构化数据，不负责最终 Markdown 渲染。
- 新增测试：`tests/test_http_client.py`、`tests/test_market_data_skill.py`，并补充 formatter/runtime 结构化结果测试。
- 当前完整测试结果：64 项通过。

## 2026-06-05 证券行情表格与字段兜底修复

- 根因 1：Agent 查询“最新股价”时可能传入 `fields: ["最新股价"]`，证券技能旧逻辑只识别英文标准字段，导致价格字段被过滤掉，最终显示 `-`。
- 修复：`agent_system/skills/local/query_market_data.py` 新增字段别名和兜底逻辑，无法识别字段时回退到默认行情字段，保证最新价/收盘价、开盘价、最高价、最低价等不会被误删。
- 根因 2：前端 `reply-card` 只是 `textContent + pre-wrap`，Markdown 表格按纯文本显示，在电脑端也会显得不对齐。
- 修复：`index.html` 新增轻量 Markdown 渲染，支持一级/二级/三级标题、`**加粗**` 和 Markdown 表格；表格渲染为真实 HTML table。
- 桌面端优化：助手消息区域从固定 `760px` 放宽到 `min(1120px, calc(100vw - 380px))`，证券表格在电脑端更容易阅读。
- 实测：`local.query_market_data` 查询 `300475` 且传入 `fields: ["最新股价"]`，返回 **香农芯创**、最新价/收盘价 **171.9**。
- 当前完整测试结果：65 项通过。

## 已知注意事项

- `youbestar.json` 是本地配置，不能上传 GitHub。
- 官方技能文件当前是包装模块，实际逻辑仍复用 `tools/`。
- community skill 安装还没有做导入包流程，目前只预留了目录和命名空间。
- local skill 审批通过后会注册为 `local.*`。
- 前端技能页现在只做管理和展示，不做 marketplace。

## 建议下一步

优先级从高到低：

1. 做 Skill Package 导入接口：读取 `meta.json + skill.py`
2. 做 community skill 安装/卸载
3. 支持同一技能多版本并切换默认版本
4. 增加技能测试报告 UI
5. 增加更多工具技能：Excel、OCR、网页抓取
6. 将 prompt 模板独立成配置文件

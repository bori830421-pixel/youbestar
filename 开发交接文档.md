# Youbestar Agent 开发交接文档

## 项目定位

这是一个本地可控的 AI Agent 原型系统。

当前目标不是做复杂平台，而是先完成最小闭环：

- 网页对话
- 模型配置
- 第三方 API 模型列表发现
- 闲聊模式 / 任务优先模式
- Agent Action 解析
- 技能调用
- 技能审批
- 技能注册表
- GitHub 同步上传

后续可以继续扩展 Excel、OCR、ERP、浏览器自动化和社区技能安装系统。

## 当前运行状态

后端：

```text
FastAPI + uvicorn
端口：8000
入口：server.py
```

前端：

```text
index.html
通过 FastAPI 根路径返回，也可以直接打开
```

启动：

```bat
start.bat
```

服务地址：

```text
http://127.0.0.1:8000/
```

## 主要模块说明

### `server.py`

负责：

- 返回 `index.html`
- 读写模型配置
- 接收聊天请求
- 调用 Agent loop
- 挂载技能系统 API

主要接口：

```text
GET  /health
GET  /
GET  /config
POST /config
POST /models/discover
POST /chat
```

`POST /chat` 请求体支持：

```json
{
  "message": "你好",
  "allowChat": true,
  "history": []
}
```

`allowChat=True`：允许自然回复，模型可以输出 `Response:`。

`allowChat=False`：任务优先，只允许 `Thought / Action / Params`，后端会丢弃自然回复。

### 模型列表发现

配置页填写 API 地址和 API Key 后，可以调用：

```text
POST /models/discover
```

请求：

```json
{
  "api_url": "https://api.deepseek.com/v1",
  "api_key": "..."
}
```

后端会把兼容地址转换为 `/models`，携带 Bearer Key 请求第三方接口，并统一提取模型 ID。响应不会返回 API Key：

```json
{
  "models_url": "https://api.deepseek.com/v1/models",
  "models": ["deepseek-chat", "deepseek-reasoner"]
}
```

支持 OpenAI-compatible 的 `data: [{"id": "..."}]` 和常见 `models` 列表格式。不支持模型发现的第三方接口仍可手动填写模型名。

当用户只填写供应商根地址，且根 `/models` 返回 `404` 时，后端只会再尝试一次 `/v1/models`。成功后响应中的 `api_url` 会返回实际 `/v1` 基地址，前端同步更新并自动保存，避免聊天请求继续使用错误路径。

### `core/llm.py`

负责请求 OpenAI-compatible 云端模型。

API 地址从 `youbestar.json` 读取。

大部分云端模型只要是 `/v1/chat/completions` 兼容格式，都可以接入。

### `core/loop.py`

Agent 主循环。

核心逻辑：

1. 构造 prompt
2. 要求模型输出 `Thought / Action / Params`
3. 根据 `allowChat` 决定是否允许 `Response`
4. 解析模型输出
5. 判断 Action 是否注册、是否开启
6. 调用技能
7. 返回模型输出、工具结果和可选用户回复

当前 prompt 已要求模型使用命名空间 Action：

```text
official.open_browser
local.xxx
community.author.xxx
```

### `core/parser.py`

负责解析模型输出：

```text
Thought: ...
Action: official.open_browser
Params: {"url": "https://www.baidu.com"}
Response: 你好
```

返回：

- `thought`
- `action`
- `params`
- `response`

`Response` 是可选字段，只在 Chat On 场景用于自然回复。

### `agent_system/manager.py`

技能系统核心。

负责：

- 创建目录
- 校验命名空间
- 写入 sandbox
- 写入测试
- 运行测试
- 提交审批
- 批准/拒绝
- 注册技能
- 加载并运行技能

关键设计：

```text
skills/
  official/
  community/
  local/
  registry.json
```

### `agent_system/skill_registry.py`

负责：

- 技能开关
- 技能卡片列表
- 兼容旧 Action 名称

例如旧名称：

```text
open_browser
```

会映射为：

```text
official.open_browser
```

### `agent_system/server.py`

技能系统的 FastAPI router。

主要接口：

```text
GET  /skills/prompt
GET  /skills/read-policy
POST /skills/files/list
POST /skills/files/read
GET  /skills/approved
GET  /skills/registry
POST /skills/toggle
GET  /skills/approvals
POST /skills/sandbox/write
POST /skills/tests/write
POST /skills/tests/run
POST /skills/approval/request
POST /skills/approve
POST /skills/reject
```

## 技能命名规则

必须使用命名空间：

```text
official.skill_name
community.author.skill_name
local.skill_name
```

含义：

- `official`：官方维护技能，可随项目版本升级
- `community`：社区共享技能，可下载、导入、升级
- `local`：本地用户技能，不自动更新

模型新写技能时，只允许进入：

```text
local.skill_name
```

## 技能注册表

文件：

```text
agent_system/skills/registry.json
```

示例：

```json
{
  "official.open_browser": {
    "path": "agent_system/skills/official/open_browser.py",
    "version": "1.0.0",
    "source": "official",
    "title": "打开浏览器",
    "description": "根据 URL 打开系统默认浏览器。",
    "author": "youbestar"
  }
}
```

运行逻辑：

```text
Action -> registry.json -> skill path -> import module -> run(params)
```

## 安全边界

模型允许：

- 写入 `agent_system/sandbox/`
- 写入测试
- 运行 sandbox 测试
- 提交审批
- 读取白名单内普通项目文件

模型禁止：

- 直接修改 `skills/`
- 删除文件
- 操作系统文件
- 读取密钥、token、cookie、auth 文件
- 调用 ERP、微信、发布、删除等高风险动作
- 执行未批准代码

安全扫描会拦截部分高风险 import 和函数调用。

## GitHub 交接

GitHub 仓库：

```text
https://github.com/bori830421-pixel/youbestar
```

两个 BAT 是相互独立的单向操作：

- `github_upload.bat`：以本地版本为准，上传并覆盖 GitHub。
- `github_sync.bat`：以 GitHub 为来源，只下载同步。

上传脚本会：

- 初始化 `.git`
- 设置远端 origin
- 设置分支为 `main`
- 检查 `youbestar.json` 是否被跟踪
- add / commit
- 刷新远端分支元数据，但不会 pull
- 使用 `git push --force-with-lease` 安全覆盖远端

下载同步脚本会：

- 初始化 `.git`
- 设置远端 origin
- 检查未提交的本地修改；发现修改立即停止并保护文件
- 检查远端 main 是否存在
- fetch
- 使用 `pull --ff-only` 下载；历史分叉时停止，不自动改写历史
- 永远不会 push

空仓库第一次没有远端 `main` 分支，需要先运行上传脚本。

## 当前测试

测试目录：

```text
tests/
```

运行：

```bat
.venv\Scripts\python.exe -m unittest discover -s tests
```

当前测试项：

- parser 解析 `Thought / Action / Params`
- skill namespace 规范化
- registry 注册和执行
- Agent loop 处理未注册 Action
- 第三方模型接口错误声明字符集时，仍按 UTF-8 正确解析中文 JSON
- 前端对话列表刷新/重开页面后可从浏览器本地存储恢复
- official.query_weather 天气技能注册、调用和结果格式化
- official.install_local_skill 可直接安装/覆盖 local.* 技能
- 工具执行结果可桥接为用户自然回复
- 自然闲聊默认只展示用户可见最终回复，调试过程默认隐藏
- Youbestar 自研 Agent Runtime 骨架已建立
- official.write_project_file 可写入项目内普通文件
- core/ui_formatter.py 统一 Agent 用户可见输出格式

## 2026-06-04 中文乱码修复

- 根因：部分 OpenAI 兼容中转接口返回 UTF-8 JSON，但响应头没有正确声明字符集，`requests.Response.json()` 会产生中文乱码。
- 修复：`core/llm.py` 直接从响应原始字节按 `utf-8-sig` 解析 JSON。
- 回归测试：`tests/test_llm.py`
- 当前完整测试结果：24 项通过。

## 2026-06-04 对话记录持久化修复

- 根因：前端 `conversations` 只保存在页面内存中，关闭网页或刷新后会丢失。
- 修复：`index.html` 使用 `localStorage` 保存对话列表、当前选中对话和消息历史；重新进入应用时自动恢复。
- 细节：保存时过滤 `pending` 临时消息，避免把“正在思考...”状态持久化。
- 附带修复：`agent_system/manager.py` 的 `to_relative()` 兼容 Windows 临时目录长路径/短路径名不一致，避免完整测试在注册技能用例中误报。
- 回归测试：`tests/test_conversation_persistence_ui.py`
- 当前完整测试结果：26 项通过。

## 2026-06-04 天气查询技能

- 新增官方技能：`official.query_weather`
- 技能入口：`agent_system/skills/official/query_weather.py`
- 工具实现：`tools/weather_tool.py`
- 数据来源：Open-Meteo API，内置常用中国城市坐标，默认城市为汕头。
- 调用参数：`{"city": "汕头", "days": 7}`，`days` 限制为 1-7。
- Agent prompt 已增加规则：用户查询天气、天气预报、气温、下雨情况时调用 `official.query_weather`。
- 回归测试：`tests/test_weather_tool.py`
- 当前完整测试结果：29 项通过。

## 2026-06-04 自主安装本地技能模式

- 新增官方技能：`official.install_local_skill`
- 技能入口：`agent_system/skills/official/install_local_skill.py`
- 工具实现：`tools/evolution_tool.py` 调用 `agent_system.manager.install_local_skill()`
- 能力：Agent 可直接写入或覆盖本地运行目录 `D:\YoubestarLocal\skills\local\` 下的 `local.*` 技能，并写入 `D:\YoubestarLocal\registries\local.registry.json` 注册为正式技能。
- Prompt 已改为：用户要求创建或改进技能时，优先调用 `official.install_local_skill`，不再要求先写 sandbox、测试、提交人工审批。
- 边界：自主安装只允许 `local.*` 命名空间，路径限制在本地运行目录的 `skills/local/`。
- 本地技能运行时不再套用旧的安全扫描；community 技能仍保留扫描。
- 回归测试：`tests/test_skill_registry.py`、`tests/test_agent_loop.py`
- 当前完整测试结果：31 项通过。

## 2026-06-04 工具结果回复桥接修复

- 根因：Agent 执行技能后只返回 `action_result`，如果模型没有提前写 `Response`，用户回复区域不会自然展示工具结果。
- 修复：新增 `core.loop.bridge_tool_result_to_response()`，在 `allowChat=True` 且存在工具 Action 时，把工具执行结果桥接为 `response`。
- 覆盖范围：普通 `/chat` Agent loop 已接入。
- 效果：查询天气这类技能调用会直接在用户回复区域返回天气结果，不再只显示在工具卡片里。
- 回归测试：`tests/test_agent_loop.py`
- 当前完整测试结果：33 项通过。

## 2026-06-04 自然聊天展示修复

- 根因：后端 `reply` 和前端主气泡都偏调试日志风格，会把 `模型输出 / 工具执行结果 / Thought / Action / Params` 暴露给用户，闲聊显得冷冰冰。
- 修复：`server.py` 的 `reply` 改为用户可见最终答案；调试信息继续保留在 `model_reply`、`thought`、`action`、`params`、`action_result` 字段。
- 前端：`index.html` 默认关闭思考和工具过程展示，主气泡优先显示 `response` / `reply` / `content`，调试按钮打开后仍可查看 Thought、Action、Params、Result。
- Prompt：`core/loop.py` 明确要求 Response 是直接给用户看的自然、简洁、温暖回复，不要提内部协议字段。
- 回归测试：`tests/test_chat_reply.py`、`tests/test_natural_chat_ui.py`、`tests/test_agent_loop.py`
- 当前完整测试结果：40 项通过。

## 2026-06-04 旧图框架实验记录

- 当时曾做过图框架旁路实验，用于验证状态图和执行后复盘思路。
- 2026-06-06 已按产品方向移除：删除旁路聊天接口、桥接文件、前端开关、相关依赖和测试。
- 后续不要恢复旁路实验入口；执行后复盘、搜索重试、记忆闭环等能力应继续沉到自研 `AgentRuntime`。

## 2026-06-04 Youbestar 自研 Agent Runtime 骨架

- 战略定位：Youbestar 最终要实现自己的独立智能体内核，不再保留图框架旁路实验入口。
- 架构文档：`docs/architecture/youbestar-agent-runtime.md`
- 新增状态层：`core/agent_state.py`
- 新增节点层：`core/agent_nodes.py`
- 新增运行时：`core/agent_runtime.py`
- 新增检查点接口：`core/agent_checkpoint.py`
- 当前最小自研流程：`prepare -> execute -> reflect -> finalize`
- 行为：复用现有 prompt、parser、技能注册表、技能开关和工具执行；执行后通过 reflect 节点复盘结果；finalize 输出兼容现有 chat 响应字段的 `AgentResult`。
- `/chat` 已默认切换到自研 `AgentRuntime`；旧 `agent_loop` 仍作为兼容 fallback 保留。
- 回归测试：`tests/test_agent_runtime.py`
- 当前完整测试结果：45 项通过。

## 2026-06-04 `/chat` 接入自研 Agent Runtime

- 入口：`server.py`
- 默认行为：`/chat` 使用 `AgentRuntime().run(...)`，执行 `prepare -> execute -> reflect -> finalize`。
- 兼容退路：`run_legacy_agent_loop()` 保留旧 `agent_loop` 包装；`USE_AGENT_RUNTIME` 控制默认路径。
- 线程支持：`ChatRequest` 新增 `threadId` 字段，前端传入的对话 ID 可进入自研 Runtime。
- API 兼容：返回字段继续保持 `reply / model_reply / thought / action / params / action_result / response`。
- 回归测试：`tests/test_server_chat_runtime.py`
- 当前完整测试结果：49 项通过。

## 2026-06-04 项目文件写入技能

- 新增官方技能：`official.write_project_file`
- 技能入口：`agent_system/skills/official/write_project_file.py`
- 工具实现：`tools/file_access_tool.py` 调用 `agent_system.file_access.write_allowed_file()`
- 能力：允许 Agent 写入运行项目白名单目录内的普通文本或代码文件。
- 参数：`{"path": "docs/notes.md", "content": "...", "overwrite": false}`
- 安全边界：仍禁止写入 `.git`、`.venv`、`youbestar.json`、`.env`、token、cookie、credential、secret 等敏感路径或敏感命名文件。
- Prompt 已增加规则：用户要求直接在运行目录、项目目录或指定普通文件中写入/修改内容时，调用 `official.write_project_file`。
- 回归测试：`tests/test_project_file_write.py`、`tests/test_agent_loop.py`
- 当前完整测试结果：54 项通过。

## 2026-06-05 结构化输出格式层

- 根因：工具结果虽然已经执行成功，但主回复可能只显示模型的空泛说明，例如“正在为你查询...”，真实结果需要展开工具卡片才看得到。
- 新增模块：`core/ui_formatter.py`
- 提供函数：`format_order_result(data)`、`format_inventory(data)`、`format_error(msg)`、`format_agent_reply(action, response, action_result)`
- 输出规范：必须有一级标题；复杂内容拆二级模块；重点加粗；多条数据用 Markdown 表格；emoji 控制在标题和少量提示中。
- 接入位置：自研 `AgentRuntime`、`server.py` 最终用户回复构造。
- 天气结果：`official.query_weather` 的多日结果会在最终回复中转成表格，不再只藏在工具调用卡片里。
- 回归测试：`tests/test_ui_formatter.py`、`tests/test_agent_runtime.py`、`tests/test_chat_reply.py`
- 当前完整测试结果：59 项通过。

## 2026-06-05 项目规则固化：技能输出与联网层

- 新增根目录规则文件：`AGENTS.md`
- 固化原则 1：技能只负责业务执行并返回结构化数据，不负责最终 Markdown 渲染；最终用户可见回复统一走 `core/ui_formatter.py`。
- 固化原则 2：联网技能不各自处理编码、请求头、超时和 JSON 解析；后续应新增或复用 `core/http_client.py`，统一提供 `fetch_text()`、`fetch_json()`。
- 编码策略：共享网络层负责从响应头识别 charset，并自动尝试 `utf-8-sig`、`utf-8`、`gb18030`、`gbk`，避免每个技能手写“某接口用 GBK”。
- 固化原则 3：技能按能力域归类，不无限平铺增长。建议域包括 `query_hub`、`market_data`、`weather_data`、`web_search`、`browser_headless`、`browser_desktop`。
- 浏览器能力拆分：桌面可见浏览器用于打开页面、登录、人工查看；无头浏览器用于后台搜索、抓取、提取和校验。
- 本次为文档/规则固化，未改业务代码。

## 2026-06-05 联网层与结构化技能结果改造

- 新增共享网络层：`core/http_client.py`
- 提供能力：`fetch_text()`、`fetch_json()`、`decode_bytes()`，统一处理请求头、超时、响应头 charset、`utf-8-sig` / `utf-8` / `gb18030` / `gbk` fallback 和 JSON 解析。
- `tools/weather_tool.py` 已改为使用 `fetch_json()`。
- 证券行情技能已从 `local.query_market_data` 迁移为官方 `official.query_market_data`。
- 官方证券技能使用东方财富轻量接口，支持 `symbol` 传入代码或中文名称，例如 `600519`、`贵州茅台`、`中国太保`，不通过浏览器爬取页面，也不支持 `function/api` 股票扩展包装。
- 证券技能不返回手写 Markdown，而是返回结构化对象：`ok / kind / title / columns / rows / summary / data`。
- `core/ui_formatter.py` 新增 `format_skill_result(result)` 和 `observation_to_text(value)`，可统一渲染结构化技能结果，避免主回复显示 Python dict。
- 自研 `AgentRuntime` 的 `execute` 节点不再 `str(run_approved_skill(...))`，保留原始结构化结果；`finalize` 再统一格式化。
- `core/loop.py` prompt 已补充规则：Agent 自己创建联网技能时必须复用 `core.http_client`；技能只返回结构化数据，不负责最终 Markdown 渲染。
- 新增测试：`tests/test_http_client.py`、`tests/test_market_data_skill.py`，并补充 formatter/runtime 结构化结果测试。
- 当前完整测试结果：64 项通过。

## 2026-06-05 证券行情表格与字段兜底修复

- 根因 1：Agent 查询“最新股价”时可能传入中文股票名或自然语言字段，证券技能旧逻辑只按代码和本地字段别名处理，容易走错技能路径或显示 `-`。
- 修复：`tools/stock_tool.py` 通过东方财富搜索接口按中文名称/代码匹配，再用实时行情接口返回最新价、涨跌幅、今开、最高价、最低价等核心字段。
- 根因 2：前端 `reply-card` 只是 `textContent + pre-wrap`，Markdown 表格按纯文本显示，在电脑端也会显得不对齐。
- 修复：`index.html` 新增轻量 Markdown 渲染，支持一级/二级/三级标题、`**加粗**` 和 Markdown 表格；表格渲染为真实 HTML table。
- 桌面端优化：助手消息区域从固定 `760px` 放宽到 `min(1120px, calc(100vw - 380px))`，证券表格在电脑端更容易阅读。
- 实测：`official.query_market_data` 支持中文名称查询，例如 **贵州茅台**、**中国太保**，返回结构化证券行情表格。
- 当前完整测试结果：65 项通过。

## 2026-06-08 股票查询轻量化

- 用户反馈：原股票查询接口设计过大，AkShare 包装函数导致模型容易思考过久并偏离“股票查询直接返回行情”的初衷。
- 修复：删除 `tools/stock_tool.py` 中的 AkShare 包装函数，改为复用桌面脚本的东方财富两步查询：`searchapi.eastmoney.com` 搜索股票，`push2.eastmoney.com` 获取实时行情。
- `official.query_market_data` 只支持股票代码或中文名称；显式 `function/api` 参数返回不支持，避免历史行情、板块、资金流等扩展重新进入生产路径。
- `requirements.txt` 移除 `akshare` 依赖；技能描述、runtime prompt、README 和 AGENTS 规则已同步。
- 验证：`.\\.venv\\Scripts\\python.exe -m unittest tests.test_market_data_skill`，4 项通过。

## 2026-06-08 本地函数快路径

- 目标：减少智能体思考时间，让股票、天气等常用外部数据查询秒级返回。
- Runtime 新增 `local_tool_intent` 节点，位于理解节点之前；命中股票/天气意图时直接设置 `action/params`，不调用 LLM 做理解、规划或自检。
- 本地工具函数：`tools.stock_tool.get_stock_price(symbol)` 返回标准化股票字典；`tools.weather_tool.get_weather(city)` 返回标准化天气字典和简单提醒。
- `official.query_market_data` / `official.query_weather` 作为薄包装技能，返回结构化结果并交给 `core.ui_formatter.py` 渲染。
- Prompt 和 AGENTS 规则已明确：股票、天气、股市行情等已有本地函数工具的查询禁止退化为网页搜索或浏览器打开。
- 验证：`.\\.venv\\Scripts\\python.exe -m unittest tests.test_agent_runtime tests.test_market_data_skill tests.test_weather_tool`，23 项通过。

## 2026-06-08 本地快路径参数修复

- 问题：`深圳未来三天天气` 被快路径固定成 `days=1`；`贵州茅台最新股价`、`601601最新收盘价` 会把 `最新` 留进 `symbol`，导致股票接口查不到。
- 修复：`core.agent_nodes._infer_weather_days()` 支持阿拉伯数字和中文一到七天；`_infer_stock_symbol()` 优先提取 6 位代码，否则清理 `最新/当前/今天/股价/收盘价/行情` 等修饰词。
- 验证：`.\\.venv\\Scripts\\python.exe -m unittest tests.test_agent_runtime`，18 项通过。

## 2026-06-08 天气固定七天查询与本地筛选

- 设计决定：天气工具每次固定请求 7 天天气数据，本地按用户输入的 1/3/5/7 天截取返回，避免把筛选交给模型造成额外耗时或答错天数。
- `tools.weather_tool.query_weather()` 返回结构化 `weather_forecast` 表格数据；`get_weather(city)` 仍提供第一天的标准化天气字典给简单天气场景复用。
- 验证：`.\\.venv\\Scripts\\python.exe -m unittest tests.test_agent_runtime tests.test_market_data_skill tests.test_weather_tool tests.test_agent_loop tests.test_ui_formatter`，44 项通过。

## 2026-06-08 证券实时行情兜底修复

- 问题：东方财富搜索接口可返回股票代码和市场，但 `push2.eastmoney.com` 实时行情接口在当前运行环境会断开连接或返回 503，导致 `601601`、`贵州茅台` 查询失败。
- 修复：保留东方财富搜索用于中文名/代码解析；实时行情优先尝试东方财富，失败时调用腾讯轻量行情接口 `qt.gtimg.cn` 兜底。仍然是本地函数直接调用，不走浏览器、网页搜索或 AkShare。
- `tools.stock_tool.get_stock_price(symbol)` 现在保留 `数据来源`；最终表格会显示真实来源，例如 `腾讯行情接口`。
- 验证：`.\\.venv\\Scripts\\python.exe -m unittest tests.test_market_data_skill tests.test_agent_runtime`，24 项通过；真实查询 `601601` 和 `贵州茅台` 均返回行情。

## 2026-06-08 Agent 三开关模式

- 用户反馈：旧模式只有 `allowChat`，即使只是普通问答，Agent 也会先思考是否调用工具，造成额外延迟。
- 修复：前端新增三个独立开关：`允许闲聊`、`允许工具`、`允许技能`，并用 `localStorage` 记住上次选择。
- 后端 `/chat` 请求新增 `allowTools` 和 `allowSkills`；`AgentState`、`AgentRuntime`、legacy `agent_loop` 都同步接收。
- Runtime 新增 `direct_chat` 节点：当 `allowChat=True` 且 `allowTools=False` 且 `allowSkills=False` 时，直接让模型回答，不进入理解、工具判断、执行、自检等 Agent 节点。
- 权限拦截：`official.*` 需要 `allowTools=True`；`local.*` / `community.*` 需要 `allowSkills=True`。本地天气/股票快路径也只在 `allowTools=True` 时运行。
- 验证：`.\\.venv\\Scripts\\python.exe -m unittest tests.test_agent_runtime tests.test_server_chat_runtime tests.test_agent_loop tests.test_market_data_skill tests.test_weather_tool tests.test_ui_formatter`，56 项通过；浏览器检查确认三个开关 DOM、请求字段和存储 key 存在。

## 2026-06-06 网页搜索与搜索后综合回答

### 本轮用户暴露的问题

用户连续测试了这类请求：

```text
搜索榴莲仅退款，查询是哪个地区的榴莲仅退款
最新有什么新出来的大模型
gnes 是什么？他的官方网址是
```

发现几个核心问题：

- Agent 起初只会调用 `official.open_browser` 打开百度页面，不会把答案返回给用户。
- 后来新增搜索技能后，技能仍写死百度，遇到百度 JSON 结构变化就返回空结果。
- 多源搜索成功后，搜索摘要里出现“失败”等字样，会被 `core.ui_formatter.py` 误判为执行失败。
- 即使搜索拿到了结构化结果，最终回复仍可能把 Python dict 或原始搜索表格直接展示给用户，没有像真正 Agent 一样归纳、分类、判断结论。
- 用户一次问多个子问题时，例如“是什么 + 官方网址”，综合回答可能只覆盖其中一项，导致用户要追问第二次。

### 已完成修复

新增官方搜索技能：

```text
official.web_query
```

相关文件：

```text
agent_system/skills/official/web_query.py
tools/web_query_tool.py
agent_system/skills/registry.json
agent_system/skill_registry.py
agent_system/skill_settings.json
```

当前行为：

- `official.web_query` 默认 `provider=auto`。
- 自动尝试多个搜索源：国内可访问源优先；网络环境允许时，补充尝试外网搜索引擎和信息源，例如 `bing_global`、`duckduckgo`。
- 返回结构化结果：`ok / kind / title / columns / rows / summary`。
- 搜索结果行包含“来源”列，方便后续综合节点判断信息来源。
- 百度 JSON 为空时会 fallback 到百度普通 HTML 页面。
- Bing / 搜狗 HTML 也有基础解析兜底。

Prompt 路由已更新：

- 用户要“搜索 X 并告诉我结果”“哪个地区”“这件事是什么情况”时，优先走 `official.web_query`。
- 用户问“最新热点”“最近发布”“新出来的大模型/产品/政策/新闻”这类时效问题时，也必须走 `official.web_query`。
- 只有用户明确要求“打开浏览器/打开网页/打开百度”时才走 `official.open_browser`。
- 不要在 Prompt 中指定单一搜索源，除非用户明确要求。

Runtime 已新增搜索后综合节点：

```text
understand -> prepare -> rewrite_query -> execute -> reflect -> synthesize -> answer_check -> finalize
```

相关文件：

```text
core/agent_nodes.py
core/agent_runtime.py
core/ui_formatter.py
```

当前逻辑：

- 技能只负责搜索和返回结构化资料。
- `understand` 节点会先把用户原话解析成结构化意图：`task_type / subject / sub_questions / constraints / needs_fresh_info / expected_output / query_hints`。
- `rewrite_query` 节点会在 `official.web_query` 执行前，用理解节点给出的 `query_hints` 或 `subject + sub_questions` 改写搜索词。
- `synthesize` 节点在 `official.web_query` 成功后，把搜索结果交给 LLM 二次总结。
- `answer_check` 节点会在最终输出前检查是否漏答用户的子问题；若漏答，会追加“补充说明”列出缺失点。
- 综合回答要求先给结论，再分类整理，例如地区、主体、时间、进展、证据来源。
- 综合节点会根据用户原问题推断“必须覆盖的回答点”，例如“是什么”“官方网址”“地区”“时间”“最新进展”。
- 如果用户一次问多个问题，综合回答必须逐项覆盖；资料不足时要明确说“搜索结果中暂未找到...”，不能静默漏答。
- 如果资料中出现多个地区，要区分角色，例如“涉事买家地区”“商家所在地”“事件发生地”。
- `ok: True` 的结构化结果不会再因为文本里出现“失败/错误”等词被误判为执行失败。
- `finalize` 会优先展示综合节点生成的自然答案，不再把原始 dict 或搜索表格直接甩给用户。

### 当前验证

本轮已运行：

```bat
.venv\Scripts\python.exe -m unittest tests.test_agent_runtime tests.test_server_chat_runtime tests.test_ui_formatter tests.test_web_query_tool tests.test_agent_loop tests.test_chat_reply tests.test_skill_registry
```

结果：

```text
Ran 38 tests in 0.112s
OK
```

注意：

- 之前尝试过 `pytest`，但当前虚拟环境没有安装 `pytest`，所以改用项目现有 `unittest`。
- Windows 命令偶尔会出现 `CreateProcessAsUserW failed: 1920`，按 `AGENTS.md` 规则原样提权重跑即可，不要因此改业务代码。

### 仍需继续改进

当前 `official.web_query` 还是“轻量搜索聚合”，不是完整新闻检索系统。下一步建议：

1. 把搜索 provider 拆成 provider adapter

当前 `tools/web_query_tool.py` 里直接写了 Baidu / Bing / Sogou 的 URL 构造和 HTML 解析。建议拆成：

```text
tools/web_search/
  __init__.py
  providers.py
  baidu.py
  bing_cn.py
  sogou.py
  news_sources.py
```

这样后续新增头条、腾讯新闻、澎湃、财新、36氪、机器之心等来源时，不会继续堆在一个文件里。

2. 建立“搜索 -> 阅读详情页 -> 综合”的两跳流程

现在多数答案只靠搜索结果摘要综合，容易受摘要噪声影响。建议新增 `official.read_web_page` 或内部 `fetch_detail_pages()`：

```text
web_query -> 候选结果
read_web_page -> 抽取正文/发布时间/来源
synthesize -> 基于正文综合回答
```

注意仍要走 `core.http_client.py`，不要让技能各自处理请求头、编码和超时。

3. 给搜索结果增加质量评分

建议在结构化结果中增加字段：

```json
{
  "source": "腾讯新闻",
  "title": "...",
  "snippet": "...",
  "url": "...",
  "published_at": "...",
  "source_type": "news",
  "score": 0.86,
  "reasons": ["标题命中", "摘要命中地区", "来源可信"]
}
```

综合节点可优先使用高分新闻来源，降低“推荐搜索词/页面导航/AI 摘要噪声”进入最终回答的概率。

4. 增加新闻/热点专用 query 改写

例如用户问：

```text
最新有什么新出来的大模型
```

当前会直接用用户原句作为 query。更好的做法是在 `prepare` 或独立 `query_rewrite` 节点生成多个搜索词：

```text
最新大模型 发布 中国
国产大模型 最新发布
AI 大模型 新模型 发布 2026
机器之心 大模型 发布
36氪 大模型 发布
```

然后合并搜索结果再综合。

5. 将 synthesize 节点从规则函数升级成 AgentNode 策略

目前 `synthesize_node()` 只针对 `official.web_query` 做综合。后续可以抽象为：

```text
AgentPolicy.should_synthesize(state)
AgentPolicy.build_synthesis_prompt(state)
```

这样市场行情、天气、订单、网页阅读等技能都可以按统一策略决定是否二次总结。

6. 让最终回答展示“结论 + 来源”

用户现在要的是答案，不是原始表格。但对于新闻热点，最终回答最好保留简短来源引用：

```text
结论：...

依据：
- 腾讯新闻：...
- 搜狗搜索摘要：...
```

这部分应由综合节点输出自然 Markdown，技能仍只返回结构化数据。

7. 增加端到端回归用例

建议新增测试：

```text
tests/test_web_query_synthesis.py
```

覆盖：

- 搜索成功但摘要含“失败”时，不显示执行失败。
- 搜索结果含多个地区时，综合回答区分买家所在地/商家所在地。
- “最新有什么新出来的大模型”触发 `official.web_query`，并进入 synthesize。
- “是什么 + 官方网址”这类多子问题必须逐项回答，不能只答一个。
- `synthesize` 输出不包含 Python dict，不包含原始 `ok/kind/rows`。

## 2026-06-06 语言理解、查询改写与回答自检节点

### 改造目标

用户希望智能体不只是“调用工具”，而是更聪明地理解语言。第一版改造目标是让 Runtime 显式增加三种能力：

- 先理解用户：识别任务类型、主体、子问题、约束、是否需要时效信息、搜索关键词提示。
- 再改写查询：用户表达不标准或多子问题时，自动生成更适合搜索的 query。
- 最后自检答案：回答前检查是否漏答子问题，特别是“是什么 + 官网”“地区 + 进展”这类复合问题。

### 已完成改动

相关文件：

```text
core/agent_state.py
core/agent_nodes.py
core/agent_runtime.py
tests/test_agent_runtime.py
```

新增状态字段：

```text
AgentState.intent
AgentState.answer_check
AgentResult.intent
AgentResult.answer_check
```

新增节点：

```text
understand_node
rewrite_query_node
answer_check_node
```

当前默认 Runtime 流程：

```text
understand -> prepare -> rewrite_query -> execute -> search_retry -> reflect -> synthesize -> answer_check -> finalize
```

节点职责：

- `understand_node`：调用 LLM 输出 JSON 意图结构，失败时提供兜底结构。
- `prepare_node`：沿用原有 Action 规划与 Params 解析。
- `rewrite_query_node`：只在 `official.web_query` 时生效，用 `query_hints` 或 `subject + sub_questions` 改写 `params.query`。
- `search_retry_node`：只允许最多一次二轮搜索；第一轮保留原始 query，第二轮才使用改写后的候选 query。
- `execute_node`：调用技能，仍遵守注册表和技能开关。
- `reflect_node`：处理失败和普通工具结果。
- `synthesize_node`：对搜索结果做证据综合。
- `answer_check_node`：让 LLM 检查最终回答是否覆盖必答点，漏答时追加补充说明。
- `finalize_node`：统一进入 `core.ui_formatter.py`，保持用户可见回复格式。

### 行为示例

用户问：

```text
gnes 是什么？他的官方网址是
```

理解节点应输出类似：

```json
{
  "task_type": "web_research",
  "subject": "Agnes AI",
  "sub_questions": ["Agnes AI 是什么", "Agnes AI 的官方网址是什么"],
  "constraints": ["需要官网"],
  "needs_fresh_info": true,
  "expected_output": "逐项回答",
  "query_hints": ["Agnes AI 是什么", "Agnes AI 官方网址"]
}
```

搜索前会把 query 改写为：

```text
Agnes AI 是什么 Agnes AI 官方网址
```

最终回答前会检查“是什么”和“官方网址”是否都覆盖。

### 当前验证

新增/更新测试：

```text
tests/test_agent_runtime.py
```

覆盖：

- Runtime 默认节点顺序升级为 8 个节点。
- web search 会使用 `query_hints` 改写搜索 query。
- `intent` 和 `answer_check` 会进入 `AgentResult`，便于后续调试展示。
- 多子问题搜索会逐项回答。
- 自检发现缺失项时，会追加补充说明。

已运行：

```bat
.venv\Scripts\python.exe -m unittest tests.test_agent_runtime
.venv\Scripts\python.exe -m unittest tests.test_server_chat_runtime tests.test_chat_reply tests.test_ui_formatter tests.test_web_query_tool tests.test_agent_loop tests.test_skill_registry
```

结果：

```text
Ran 9 tests ... OK
Ran 31 tests ... OK
```

## 2026-06-06 有限搜索重试与快速失败

### 设计目标

用户明确要求搜索增强必须遵守：

```text
有限规划
有限重试
快速失败
```

禁止：

- 无限循环 Agent
- 无限 Critic
- 无限 Retry
- Critic 触发搜索循环

### 已完成改动

硬限制已进入 `AgentState`：

```text
max_search_round = 2
max_tool_call = 10
max_runtime_seconds = 25.0
```

相关状态：

```text
search_round
tool_call_count
runtime_started_at
stop_reason
search_assessment
```

搜索流程：

```text
第一轮：使用原始 query
如果结果充足：直接 synthesize
如果结果不足：search_retry 使用 rewrite_query 生成的候选 query
第二轮结束：无论结果如何都停止
```

允许重试条件：

- `answer_check.missing` 或搜索评估中发现缺失子问题。
- 搜索结果数量少于 3。
- 搜索结果全部来自同一个来源。

禁止继续重试：

- 已有可信答案。
- 明显无官方信息。
- 已到第二轮。
- 已达到工具调用或运行时间上限。

达到限制时，`stop_reason` 会记录：

```text
search_limit_reached
tool_call_limit_reached
runtime_limit_reached
```

并返回结构化 partial observation：

```json
{
  "status": "partial",
  "reason": "search_limit_reached"
}
```

### 当前验证

新增测试覆盖：

- 搜索结果过少时只重试一次。
- 第二轮仍不足时停止并记录 `search_limit_reached`。
- 第一轮使用原始 query，第二轮才使用改写 query。

已运行：

```bat
.venv\Scripts\python.exe -m unittest tests.test_agent_runtime
```

结果：

```text
Ran 11 tests ... OK
```

## 2026-06-06 记忆管理框架

### 设计目标

用户要求 Youbestar 的记忆系统遵守：

```text
短期会话记忆
长期业务记忆
临时/闲聊信息
```

核心原则：

- 只有业务相关且用户确认的信息，才能进入长期记忆。
- 闲聊和一次性查询不进入长期记忆。
- 执行 ERP / Excel / 微信 / 电商业务操作时，只允许使用短期上下文 + 已确认长期业务记忆。
- 不允许脏记忆污染业务执行。

### 已完成闭环

相关文件：

```text
memory/memory.py
tests/test_memory_management.py
server.py
index.html
core/loop.py
core/agent_nodes.py
```

新增概念：

```text
Memory.history              # 短期会话记忆，默认最多 50 条
Memory.temporary            # 临时/闲聊信息，不进入模型业务上下文
Memory.pending_candidates   # 待用户确认的长期记忆候选
Memory.long_term            # 已确认长期业务记忆
MemoryRecord                # 长期记忆记录结构
```

已保留旧接口兼容：

```text
Memory.add(user_input, action, result)
Memory.get_summary()
```

新增接口：

```text
add_temporary(content, module, metadata)
clear_temporary()
detect_business_memory_candidate(user_input, action, result, module)
propose_long_term(content, memory_type, module, metadata)
confirm_pending(index=-1)
reject_pending(index=-1)
confirmed_long_term(module=None)
get_model_context(module=None)
compress_long_term()
pending_as_dicts()
long_term_as_dicts(module=None)
get_business_summary(module=None)
```

持久化文件：

```text
D:\YoubestarLocal\data\memory_long_term.json
```

注意：`Memory()` 默认不落盘，避免单元测试和临时对象污染真实业务数据；`server.py` 显式使用 `Memory(storage_path=DEFAULT_STORAGE_PATH)`。

当前允许进入长期记忆候选的类型：

```text
customer
sku
order
erp_order
finance_report
transaction
```

### 当前行为

- 短期记忆自动截断到最近 N 条，默认 50。
- 非业务类型例如 `chat` 不会进入长期记忆候选。
- 业务信息必须先进入 `pending_candidates`，只有 `confirm_pending()` 后才会进入 `long_term`。
- `get_model_context()` 只返回短期记忆和已确认长期记忆，不返回 `temporary` 或未确认候选。
- 长期记忆可按 `module` 隔离，例如 `erp_inbound` 和 `private_sales` 不互相污染。
- `compress_long_term()` 已支持基础去重，并能按 `entity + sku + qty` 聚合数量，例如“张三 买 SKU123 2件”和“张三 买 SKU123 3件”会合并为 5 件。
- `detect_business_memory_candidate()` 当前是规则型骨架，根据 ERP、SKU、订单、客户、财务、交易等关键词识别候选，只生成 `pending_candidates`，不自动固化。
- `/chat` 当前会返回 `memory_candidate` 字段，前端会弹出“是否保存到长期业务记忆？”确认框。
- 已新增后端 API：`GET /memory/context`、`POST /memory/confirm`、`POST /memory/reject`。
- 前端点击“确认保存”会调用 `POST /memory/confirm` 并写入长期记忆；点击“忽略”会调用 `POST /memory/reject`，不写入长期记忆。
- 确认和拒绝会写入 `audit_log`，随 JSON 一起持久化，便于追踪。
- `core/loop.py` 的 Prompt 已加入“已确认长期业务记忆”，并明确模型只能使用短期历史和已确认长期业务记忆。
- `core/agent_nodes.py` 的 Runtime 规划节点已直接使用 `context.memory`，不会再因为新建临时 `Memory()` 而丢失长期记忆。

### 当前验证

已运行：

```bat
.venv\Scripts\python.exe -m unittest tests.test_memory_management tests.test_agent_runtime tests.test_server_chat_runtime
.venv\Scripts\python.exe -m unittest tests.test_memory_management tests.test_server_chat_runtime tests.test_agent_loop
.venv\Scripts\python.exe -m unittest tests.test_memory_management tests.test_agent_runtime tests.test_server_chat_runtime tests.test_chat_reply tests.test_ui_formatter tests.test_web_query_tool tests.test_agent_loop tests.test_skill_registry
```

结果：

```text
Ran 20 tests ... OK
Ran 26 tests ... OK
Ran 54 tests ... OK
```

补充验证：

```bat
.venv\Scripts\python.exe -m unittest tests.test_memory_management tests.test_server_chat_runtime
```

结果：

```text
Ran 13 tests ... OK
```

覆盖：

- 短期记忆容量限制。
- 闲聊内容不进入长期记忆。
- 业务记忆必须确认后才进入模型上下文。
- 已确认长期记忆可落盘，重启后可恢复。
- 用户拒绝候选不会写入长期业务记忆，但会留下审计记录。
- 临时记忆不会进入模型上下文。
- 长期记忆按业务模块隔离。
- Agent Prompt 只包含短期历史和已确认长期业务记忆，不包含未确认候选。

### 尚未完成 / 下一步

本轮已完成最小闭环，下一阶段建议继续做：

- 可配置提示阈值，例如哪些 `memory_type` 需要提示、默认确认行为。
- 自动识别业务记忆候选的 LLM 节点；当前只有规则型骨架。
- 更强的长期记忆摘要压缩，例如历史订单、SKU、财务报表定期摘要。
- 过期任务记忆清理。
- 前端调试页展示短期 / 临时 / 待确认 / 长期记忆。
- SQLite 版本存储；当前实现是 JSON 文件，适合作为最小闭环。

### 下一位建议实现顺序

1. 增加 `memory_candidates_node`，用 LLM 或更强规则从用户输入和工具结果中识别可固化业务信息，只生成候选，不直接保存。
2. 给不同业务模块定义 module 名称，例如 `erp_inbound`、`private_sales`、`excel_report`、`chat`。
3. 增加可配置提示阈值，控制哪些 `memory_type` 会提示用户确认。
4. 增加更稳的压缩策略和清理策略，先做业务聚合，再做摘要。
5. 前端调试模式展示记忆层，但默认不展示给普通用户。
6. 如果业务量变大，把 `D:\YoubestarLocal\data\memory_long_term.json` 迁移到 SQLite。

### 继续改进建议

1. 把理解/改写/自检 prompt 从 `core/agent_nodes.py` 拆到独立配置或 `core/prompts.py`。
2. 给 `intent` 和 `answer_check` 增加前端调试展示，默认隐藏，调试模式可看。
3. 抽象 `AgentPolicy`，让“哪些 Action 需要 synthesize / answer_check / query_rewrite”不写死在节点函数里。
4. 增加短期实体记忆，例如用户第二轮说“他的官网”时，能指向上一轮主体。
5. 给 `understand_node` 增加低成本规则兜底，避免 LLM 输出非 JSON 时完全退化。
6. 增加端到端测试覆盖真实用户句式：“gnes 是什么？他的官方网址是”“最新有什么新出来的大模型”“这个公司靠谱吗”。

## 2026-06-07 模型选择下拉修复

### 本轮用户暴露的问题

登录/模型配置界面已经支持第三方模型列表发现，但前端使用原生 `datalist` 作为模型候选入口，存在两个体验问题：

- 界面只有“获取模型”按钮，没有明确的“选择模型”按钮或可见下拉入口。
- 当模型输入框已有默认值时，浏览器会按当前文字过滤 `datalist` 候选，导致点击输入框时看不到其他模型；只有先删掉模型文字，候选才容易显示。

### 已完成改动

相关文件：

```text
index.html
tests/test_model_discovery_ui.py
```

前端改动：

- 移除模型输入框对原生 `datalist` 的依赖，保留手动输入能力。
- 新增自定义模型选择面板：`#model-picker-button`、`#model-options`。
- 点击“选择模型”或聚焦模型输入框时，会展开完整候选列表，不再被默认模型文字过滤。
- 用户手动输入时才按当前文字过滤候选。
- 点击“获取模型”成功后，将后端返回的模型列表去重、排序、缓存到 `discoveredModels`，并自动展开完整候选列表。
- 选择候选模型后同步写入原有 `#model-name` 输入框，并触发现有自动保存逻辑。

测试改动：

- `tests/test_model_discovery_ui.py` 已覆盖自定义模型面板、模型获取逻辑和默认值下仍可展开完整列表的静态检查。
- 测试明确禁止退回 `list="model-options"` / `<datalist>` 方案，避免复发。

当前验证：

```bat
.venv\Scripts\python.exe -m unittest tests.test_model_discovery_ui tests.test_model_discovery
```

结果：

```text
Ran 8 tests in 0.002s
OK
```

## 2026-06-07 对话回复耗时计时器

### 本轮用户需求

用户希望对话窗口增加一个计时器：

- 用户提交消息后，可以实时看到模型从开始思考到提交答案的耗时。
- 答案提交后，这个耗时不要丢，要固定显示在答案最后面。

### 已完成改动

相关文件：

```text
index.html
tests/test_chat_ui_runtime.py
tests/test_conversation_persistence_ui.py
```

前端行为：

- `sendMessage()` 创建助手等待消息时记录 `responseStartedAt = Date.now()`。
- pending 阶段启动 `activeResponseTimer`，每秒刷新当前助手气泡。
- pending 回复卡片底部显示：`模型思考中：x.x 秒`。
- 后端返回成功或失败时计算 `responseDurationMs = Date.now() - responseStartedAt`。
- 最终回复卡片底部固定显示：`模型用时：x.x 秒`。
- `responseDurationMs` 和 `responseStartedAt` 作为助手消息 UI 元数据保存在对话历史对象中，刷新页面后最终耗时不会丢。
- 发给后端模型的 `history` 仍然只取助手原始回复字段，不包含耗时文字，避免污染模型上下文。

测试改动：

- `tests/test_chat_ui_runtime.py` 覆盖计时函数、实时计时器、最终耗时字段和 `reply-timer` 渲染。
- `tests/test_conversation_persistence_ui.py` 覆盖最终耗时字段会随非 pending 消息保存。

当前验证：

```bat
.venv\Scripts\python.exe -m unittest tests.test_chat_ui_runtime tests.test_conversation_persistence_ui tests.test_model_discovery_ui
```

结果：

```text
Ran 9 tests in 0.002s
OK
```

## 2026-06-07 API 接口配置档案

### 本轮用户需求

用户希望在登录/模型配置界面快速切换不同 API 接口，并且只要切换接口，对应的 API Key 和默认模型就会自动填入。

### 已完成改动

相关文件：

```text
core/config.py
index.html
tests/test_config_profiles.py
tests/test_model_discovery_ui.py
```

后端配置：

- `ModelConfig` 保留旧字段：`api_url / model / api_key`，继续作为当前生效配置，`/chat` 和 `LLM` 不需要改调用路径。
- 新增 `ModelProfile` 和字段：`current_profile_id / profiles`。
- 旧版 `youbestar.json` 只有单套配置时仍可读取，不会报错。
- 保存配置时，如果没有 profiles，会自动把当前配置补成一个 profile。
- 保存写入时使用 `model_dump()` / `dict()` 兼容 Pydantic v2/v1。

配置文件新格式示例：

```json
{
  "api_url": "https://api.deepseek.com/v1",
  "model": "deepseek-chat",
  "api_key": "sk-xxx",
  "current_profile_id": "deepseek",
  "profiles": [
    {
      "id": "deepseek",
      "name": "DeepSeek",
      "api_url": "https://api.deepseek.com/v1",
      "model": "deepseek-chat",
      "api_key": "sk-xxx"
    },
    {
      "id": "openai",
      "name": "OpenAI",
      "api_url": "https://api.openai.com/v1",
      "model": "gpt-4o-mini",
      "api_key": "sk-yyy"
    }
  ]
}
```

前端行为：

- 登录/模型配置页新增“接口配置”下拉框：`#profile-select`。
- 新增“保存当前接口”按钮：`#save-profile-button`。
- 选择已有接口档案时，会自动填入对应 `API 地址 / API Key / 模型名`，并保存当前选择。
- 选择“新建接口...”后可填写新接口，点“保存当前接口”后加入下拉列表。
- 获取模型和选择模型逻辑继续复用当前输入框，因此切换接口后可以直接点“获取模型”刷新对应接口的模型列表。

安全注意：

- API Key 仍只保存在本机 `youbestar.json`。
- `youbestar.json` 仍然禁止上传 GitHub。

当前验证：

```bat
.venv\Scripts\python.exe -m unittest tests.test_config_profiles tests.test_model_discovery_ui tests.test_model_discovery tests.test_llm
```

结果：

```text
Ran 13 tests in 0.008s
OK
```

## 2026-06-08 工厂报价资料技能初版

### 本轮用户需求

用户希望先把 `D:\工厂报价` 内的工厂报价资料做成可调用技能，后续支持按工厂名称 + 货号查询产品尺寸、装箱、重量、成本价，并预留图片直链、相册链接、1688 图片来源的接入。

### 已完成改动

相关文件：

```text
tools/factory_quote_tool.py
D:\YoubestarLocal\skills\local\factory_quote.py
agent_system/skills/registry.json
core/loop.py
requirements.txt
tests/test_factory_quote_tool.py
tests/test_agent_loop.py
```

能力说明：

- 新增 `local.factory_quote` 本地技能，属于“插件/技能”页展示和 Agent 调用能力，不属于“自动化”定时任务。
- `tools/factory_quote_tool.py` 负责核心业务逻辑，技能文件只做薄封装，方便后续给网页端或批量导入复用。
- 当前支持从 Excel 报价表解析产品行，默认来源为 `D:\工厂报价`，会忽略 `~$` 开头的 Excel 临时锁文件。
- 已新增本地 SQLite 报价资料库：`D:\YoubestarLocal\data\factory_quotes.sqlite3`。执行 `operation=import` 后会把 Excel 固化进资料库，日常查询优先读取 SQLite；只有资料库未创建、显式传入 `source_path/workbook_path/use_excel` 时才解析 Excel。
- 产品唯一身份使用系统生成的 `id`，业务查询优先使用 `factory_name + sku`；单独货号命中多工厂时返回多条并提示需要补充工厂。
- 结构化返回字段包含工厂、货号、品名、系列、装箱数、成本单价、成本箱价、产品尺寸、包装尺寸、外箱尺寸、毛重、含税、含运费。
- 支持基础数量报价参数：`quantity / margin_rate / pricing_tiers / tax_rate / include_tax / include_freight / freight_fee`。
- 图片资产入口：
  - `operation=image_candidate|1688|album`：生成待确认图片候选，并写入 SQLite 的 `image_assets` 表。
  - `operation=bind_image|confirm_image_binding|attach_image`：按工厂 + 货号查到唯一产品后，直接写入 confirmed 图片资产并绑定到产品。
  - 当前不会主动抓取 1688 页面；用户给图片直链、相册链接或 1688 图片链接时可绑定。
- 报价查询现在会读取 confirmed 图片资产，并在返回表格中增加“图片”列；`index.html` 已支持 Markdown 图片缩略图渲染，点击缩略图可打开原图。
- `core/loop.py` 已把已注册本地技能的 title/description 放进 Agent prompt，并增加工厂报价类问法路由说明。
- 新增依赖 `openpyxl>=3.1,<4`。
- `.gitignore` 已忽略 `data/`，避免长期记忆、报价库和本地业务数据被上传 GitHub。

真实资料验证：

```bat
.venv\Scripts\python.exe -c "from agent_system.manager import run_approved_skill; import json; result=run_approved_skill('local.factory_quote', {'operation': 'import', 'source_path': r'D:\工厂报价'}); print(json.dumps(result.get('summary'), ensure_ascii=False, indent=2))"
```

结果摘要：

```json
{
  "工厂数": 1,
  "产品数": 121,
  "资料库": "D:\YoubestarLocal\data\factory_quotes.sqlite3",
  "状态": "已写入本地报价资料库，未写入长期记忆"
}
```

查询验证：

```bat
.venv\Scripts\python.exe -c "from agent_system.manager import run_approved_skill; import json; result=run_approved_skill('local.factory_quote', {'source_path': r'D:\工厂报价', 'factory_name': '潘多多', 'sku': 'QQL701A', 'quantity': 100, 'margin_rate': 0.2}); print(json.dumps({'ok': result.get('ok'), 'summary': result.get('summary')}, ensure_ascii=False, indent=2))"
```

结果摘要：

```text
ok=True
货号=QQL701A
产品尺寸=28.9*28.9*2.3
装箱数=36
成本单价=17.745
报价单价=21.294
报价总额=2129.4
```

图片绑定验证：

```bat
.venv\Scripts\python.exe -c "from agent_system.manager import run_approved_skill; import json; url='https://cbu01.alicdn.com/img/ibank/O1CN01Orvy1O2KgRmM7gsgZ_!!2220832609586-0-cib.jpg'; result=run_approved_skill('local.factory_quote', {'operation':'bind_image','factory_name':'潘多多','sku':'PD1102','image_url':url}); print(json.dumps({'ok': result.get('ok'), 'kind': result.get('kind'), 'summary': result.get('summary')}, ensure_ascii=False, indent=2))"
```

结果摘要：

```text
ok=True
kind=image_asset_binding
货号=PD1102
状态=已绑定
```

资料库查询验证：

```bat
.venv\Scripts\python.exe -c "from agent_system.manager import run_approved_skill; import json; result=run_approved_skill('local.factory_quote', {'factory_name':'潘多多','sku':'QQL701A','quantity':100,'margin_rate':0.2}); print(json.dumps({'ok': result.get('ok'), 'source': result.get('data', {}).get('source'), 'summary': result.get('summary')}, ensure_ascii=False, indent=2))"
```

结果摘要：

```text
ok=True
source=library
货号=QQL701A
报价总额=2129.4
```

当前验证：

```bat
.venv\Scripts\python.exe -m unittest tests.test_factory_quote_tool
.venv\Scripts\python.exe -m unittest tests.test_agent_loop tests.test_skill_registry
```

结果：

```text
Ran 5 tests in 0.006s
OK

Ran 6 tests in 0.205s
OK

Ran 13 tests in 0.038s
OK

Ran 50 tests in 0.079s
OK
```

## 2026-06-09 中转接口与接口命名

用户反馈：

- OpenAI 风格中转配置包含 `model_provider / model_providers / base_url / wire_api = "responses"`，Youbestar 只按 `/chat/completions` 调用，导致接口不被识别。
- 登录/模型配置页新建接口时不能手动命名，只能从 URL 自动生成名称。

修复：

- `ModelConfig` / `ModelProfile` 新增 `name` 和 `wire_api`，兼容旧 `api_url / model / api_key` 配置。
- 后端支持从 OpenAI provider 风格配置读取 `model_provider`、`model_providers.*.base_url` 和 `wire_api`，映射到 Youbestar 的 `name / api_url / wire_api`。
- `core/llm.py` 按 `wire_api` 分流：
  - `chat_completions`：调用 `{api_url}/chat/completions`，发送 `messages`。
  - `responses`：调用 `{api_url}/responses`，发送 `instructions + input`，支持从 `output_text` 或 `output[].content[].text` 提取回复。
- `core/model_discovery.py` 现在能把 `/responses` 端点规范到 `/models`，模型列表发现仍然使用 OpenAI-compatible `/models`；不支持 `/models` 的中转仍可手动填写模型名。
- 追加修复：根地址模型发现现在优先尝试 `/v1/models`，再尝试 `/models`；第一个候选请求失败、HTTP 错误或返回非模型 JSON 时，会继续尝试下一个候选，并在失败信息里列出已尝试 URL。
- 登录页新增“接口名称”和“接口协议”字段；保存接口、切换接口、当前模型显示都会保留名称和协议。

用户填写示例：

```text
接口名称：OpenAI 中转
API 地址：http://43.133.32.30
接口协议：Responses
模型名：gpt-5.5
API Key：用户自己的中转 Key
```

如果中转文档明确要求 `/v1/responses`，API 地址填 `http://43.133.32.30/v1`；如果中转配置里的 `base_url` 就是根地址，则填根地址。

验证命令：

```bat
.venv\Scripts\python.exe -m unittest tests.test_config_profiles tests.test_llm tests.test_model_discovery tests.test_model_discovery_ui
```

## 2026-06-09 工厂报价图片类型与报价表展示

用户确认报价资料库图片分两类：

- `SKU图` / 主图：每个产品只保留一张，显示在报价查询主表第一列。
- `实拍图`：每个产品可绑定多张，保存在结构化 `real_photos` 数据里，不挤进主报价表。

修复：

- `tools/factory_quote_tool.py` 将图片类型规范为 `sku_image` / `real_photo`。
- `bind_image` 默认绑定 `sku_image`；用户明确说“实拍图/实拍照片”时传 `image_type="real_photo"`。
- 重新绑定 SKU 图会把同产品旧 SKU 图标记为 `replaced`，不会影响已绑定的实拍图。
- 报价查询主表去掉“工厂”列，第一列改为 `SKU图`，并加入 `箱规尺寸`、`净重`。
- 查询结果摘要保留工厂用于内部消歧和关键信息，但主表不显示工厂。
- `core/ui_formatter.py` 不再把 Markdown 图片单元格加粗，保证前端能把 `![SKU图](url)` 渲染成缩略图。
- `core/loop.py` 的 Agent 路由提示已加入 `image_type` 规则。

验证命令：

```bat
.venv\Scripts\python.exe -m unittest tests.test_factory_quote_tool tests.test_ui_formatter tests.test_agent_loop
.venv\Scripts\python.exe -m unittest tests.test_memory_management tests.test_server_chat_runtime tests.test_natural_chat_ui
```

结果：

```text
Ran 28 tests in 0.322s
OK

Ran 24 tests in 0.009s
OK
```

注意：第二组测试第一次在普通沙箱下因 Windows 临时目录写入权限失败；按同一命令提升权限重跑后通过。

## 2026-06-09 工厂报价表头联系人导入

用户指出 `D:\工厂报价\潘多多产品报价单更新.xlsx` 前 3 行表头已经包含业务联系人和联系电话，但 Agent 查询时说没有找到。

确认真实文件：

- A1: `潘多多（汕头）科教实业有限公司 / 产品报价表-中文系列 / 业务员：潘小姐 13502760103`
- 第 3 行是产品字段表头。

修复：

- `tools/factory_quote_tool.py` 新增报价表头元数据解析：
  - `business_contact`
  - `business_phone`
  - `raw`
- 导入 Excel 时将联系人写入每条产品记录的 `factory_contact`，继续存放在 `D:\YoubestarLocal\data\factory_quotes.sqlite3` 的 `factory_products.data_json`，不写入长期聊天记忆。
- 单个产品查询摘要现在会带出 `业务联系人` 和 `业务联系电话`。
- 新增 `operation="contact"` / `query_contact` / `factory_contact`，可只按工厂名查询业务联系人。
- `core/loop.py` 路由规则已加入 `业务员/业务联系人/业务电话/联系电话`，只问联系人时提示模型使用 `operation=contact`。

真实库已重新导入：

```bat
.venv\Scripts\python.exe -c "from tools.factory_quote_tool import import_factory_quotes; import json; r=import_factory_quotes({'source_path': r'D:\工厂报价'}); print(json.dumps(r.get('summary'), ensure_ascii=False, indent=2))"
```

结果：

```json
{
  "工厂数": 1,
  "产品数": 121,
  "资料库": "D:\YoubestarLocal\data\factory_quotes.sqlite3",
  "状态": "已写入本地报价资料库，未写入长期记忆"
}
```

验证联系人查询：

```json
{
  "工厂": "潘多多（汕头）科教实业有限公司",
  "业务联系人": "潘小姐",
  "业务联系电话": "13502760103",
  "匹配联系人数量": 1
}
```

验证命令：

```bat
.venv\Scripts\python.exe -m unittest tests.test_factory_quote_tool tests.test_agent_loop tests.test_ui_formatter
```

结果：

```text
Ran 35 tests in 0.227s
OK
```

## 2026-06-09 工厂报价资料库规格手动修正

用户确认潘多多 `PD1102` 的 Excel 原始表曾把 `产品尺寸（cm）` 和 `包装规格(cm)` 两列填反，需要修正本地知识库。

本次新增：

- `tools/factory_quote_tool.py` 新增 `operation="update_specs"`：
  - 支持写入 `product_size_cm`
  - 支持写入 `package_size_cm`
  - 支持写入 `carton_size_cm`
- Agent 路由提示已加入：用户说产品尺寸/包装尺寸/包装规格/箱规填错、要修改、要写入资料库、调换过来时，使用 `local.factory_quote` 且 `operation=update_specs`。
- 规格写回 `D:\YoubestarLocal\data\factory_quotes.sqlite3` 的产品记录，不写入长期聊天记忆。

已执行真实资料库修正：

```json
{
  "operation": "update_specs",
  "factory_name": "潘多多",
  "sku": "PD1102",
  "product_size_cm": "28.5*15*3",
  "package_size_cm": "28.5*30*3"
}
```

修正后查询结果：

```json
{
  "货号": "PD1102",
  "产品尺寸": "28.5*15*3cm",
  "包装尺寸": "28.5*30*3cm",
  "箱规尺寸": "62*33*41cm"
}
```

注意：如果未来重新从 Excel 全量导入，而 Excel 源文件仍是旧值，会覆盖资料库中的手动修正。需要确保源 Excel 已保存正确，或后续增加“手动修正优先于 Excel 导入”的 overlay 层。

验证命令：

```bat
.venv\Scripts\python.exe -m unittest tests.test_factory_quote_tool tests.test_agent_loop tests.test_ui_formatter
```

结果：

```text
Ran 36 tests in 0.221s
OK
```

## 2026-06-09 工厂报价重量语义与单品包装重量

用户确认报价表里的 `毛重/净重` 是一箱重量，不是单个产品重量：

- `箱毛重/箱净重`：沿用 Excel 原始箱重，单位 kg。
- `单品毛重/单品净重`：优先使用产品资料库手动确认值；没有确认值时按 `箱重 ÷ 装箱数 × 1000` 换算为 g，并标注“约/箱重换算”。
- `快递包装重量`：单品加快递箱/外包装后的重量，只能手动写入，后续用于运费计算；没有记录时显示“未记录”。
- 尺寸默认单位 cm；价格默认人民币元，展示精确到小数点后 2 位，使用四舍五入到分。

修复：

- `tools/factory_quote_tool.py` 新增产品重量 profile：
  - `single_net_weight_g`
  - `single_gross_weight_g`
  - `shipping_packaged_weight_g`
- 新增 `operation="update_weight"`，可将单品净重、单品毛重、快递包装重量写回 `factory_products.data_json.manual_specs`。
- 查询主表列名带单位：`产品尺寸(cm)`、`箱规尺寸(cm)`、`箱毛重(kg)`、`单品净重(g)` 等。
- 成本和报价统一按人民币两位小数展示；例如 `15.225` 展示为 `15.23元`。
- Agent 路由提示已说明：用户说“单品净重/毛重/包装重量要写入”时调用 `local.factory_quote` 且 `operation=update_weight`。

示例：

```json
{
  "operation": "update_weight",
  "factory_name": "潘多多",
  "sku": "PD1102",
  "single_net_weight_g": 350,
  "single_gross_weight_g": 370,
  "shipping_packaged_weight_g": 390
}
```

验证命令：

```bat
.venv\Scripts\python.exe -m unittest tests.test_factory_quote_tool tests.test_agent_loop tests.test_ui_formatter
```

结果：

```text
Ran 31 tests in 0.164s
OK
```

## 2026-06-09 自我进化权限总闸

用户需求：

- 在 UI 左侧新增“自我进化”入口。
- 页面中说明开启后的能力，并提供开关。
- 开启后允许智能体读取自己的代码、修改自身所在项目普通文件、创建/覆盖本地技能。

实现：

- `index.html` 新增左侧导航项“自我进化”、`#evolution-view` 页面、`#allow-self-evolution-toggle` 开关。
- 前端新增 `/self-evolution/settings` 读写，开关状态同步保存到后端，并继续写入 `localStorage` 的 Agent mode。
- 聊天请求新增 `allowSelfEvolution`，发送时随 `allowChat / allowTools / allowSkills` 一起传给 `/chat`。
- `ChatRequest`、`AgentState`、`AgentRuntime.run()`、legacy `agent_loop()` 全链路新增 `allow_self_evolution`。
- `core/loop.py` 定义 `SELF_EVOLUTION_ACTIONS`，包括 `official.list_files`、`official.read_file`、`official.write_project_file`、`official.write_skill`、`official.write_skill_test`、`official.request_skill_approval`、`official.install_local_skill`。
- Runtime 和 legacy loop 在执行前检查：关闭时返回 `自我进化未开启：{action}`，不调用技能。
- 新增 `agent_system/evolution_policy.py`，持久化 `self_evolution_settings.json`，并提供项目级 `require_self_evolution_enabled()`。
- `/skills/files/list`、`/skills/files/read`、`/skills/sandbox/write`、`/skills/tests/write`、`/skills/tests/run`、`/skills/approval/request`、`/skills/approve`、`/skills/reject` 现在也需要自我进化总闸开启，避免绕过 `/chat`。
- `/chat` 采用双重同意：前端请求 `allowSelfEvolution=true` 且后端设置开启，才传入 Runtime。
- `agent_system/file_access.py` 继续保护敏感路径，并补充 `venv/db/database/storage/profile/sqlite` 等拦截规则。
- `install_local_skill()` 现在也调用 `assert_code_safe()`，避免自我进化开启后直接安装包含危险导入/调用的 local 技能。

仍然保护：

- `.env`
- `youbestar.json`
- `.git`
- `.venv` / `venv`
- `data` / `db` / `database` / `storage`
- token / cookie / credential / secret / password / sqlite 等命名文件

验证命令：

```bat
.venv\Scripts\python.exe -m unittest tests.test_agent_loop tests.test_agent_runtime tests.test_server_chat_runtime tests.test_natural_chat_ui tests.test_self_evolution_policy tests.test_project_file_write tests.test_skill_registry
```

结果：

```text
Ran 63 tests in 0.079s
OK
```

注意：普通沙箱第一次运行会因 Windows 用户 Temp 写入权限失败；按同一命令提升权限重跑通过。

## 2026-06-09 通用 Excel 表格处理分类系统

用户要求把当前 Excel 文档整理能力从“工厂报价/报价表数据库”升级为“通用 Excel 表格处理分类系统”。系统需要根据用户上传的不同 Excel 表头，识别表格类型、字段映射、标准化和归档；不允许未识别字段被胡乱拼凑成已有字段；字段扩展建议必须弹窗或明确确认后才生效。

涉及文件：

```text
tools/excel_table_classifier.py
tools/factory_quote_tool.py
tests/test_excel_preview_tool.py
tests/test_factory_quote_tool.py
README.md
AGENTS.md
```

本次核心规则：

- Excel 预览/分类统一走 `official.preview_excel`，先读取所有工作表、表头前置行、检测表头和前 20 行样例，再分类，不直接导入数据库。
- 支持表格类型：报价表、订单表、库存表、客户表、采购表、财务表、物流表、商品资料表。
- 字段映射返回 `mapping_score`、`score_gap`、`candidates`、`needs_confirmation`。
- 自动映射阈值：`AUTO_MAPPING_SCORE = 0.85`。
- 待确认阈值：`PENDING_MAPPING_SCORE = 0.65`。
- 候选分差小于 `AMBIGUOUS_SCORE_GAP = 0.12` 时，进入待确认/模糊状态。
- 未识别字段输出 `unknown` / `unmapped`，并生成 `change_proposals`；字段目录扩展必须等用户确认，不自动写入。
- `品牌价` 明确映射为 `cost_unit_price` 成本单价，不是 `brand`。
- `包装规格` 含义不稳定，候选包括 `product_size_cm`、`package_size_cm`、`dimension_text`，默认需要确认。
- `棋盘规格` 优先候选 `product_size_cm`，也保留 `product_spec` 候选。
- `毛/净重(KG)`、`毛净重` 等合并字段保留 `weight_text`，报价导入时可解析成箱毛重/箱净重。
- `单品克重` 没说明毛/净时进入待确认，候选包括单品毛重、单品净重和重量原文。

新增或明确的标准字段：

```text
standard_price              标准售价
wholesale_price             批发价
output_tax_rate             销项税率
input_tax_rate              进项税率
short_name                  商品简称
sku_code                    规格编码
toy_type                    玩具类型
product_spec                产品规格
product_size_cm             产品尺寸(cm)
package_size_cm             包装尺寸(cm)
inner_box_quantity          内盒数量
inner_box_size_cm           内盒尺寸(cm)
carton_size_cm              外箱尺寸(cm)
carton_gross_weight_kg      箱毛重(kg)
carton_net_weight_kg        箱净重(kg)
single_gross_weight_g       单品毛重(g)
single_net_weight_g         单品净重(g)
shipping_packaged_weight_g  快递包装重量(g)
dimension_text              尺寸原文
weight_text                 重量原文
```

报价资料库兼容升级：

- `tools/factory_quote_tool.py` 继续作为报价资料库导入/查询工具，但只在用户明确要求“导入/写入资料库”时使用。
- 导入前必须识别至少一个业务身份字段：`factory_name` 或 `brand`。两者都没有时返回 `factory_quote_import_identity_required`，不写 SQLite。
- `brand` 已进入报价资料结构、SQLite 字段、查询过滤和导入汇总。
- 用户传入 `factory_name` / `brand` 参数时只作为确认后的 fallback；如果 Excel 表头能检测到完整工厂/品牌，保留表头里的完整身份。比如查询参数“吉贵”不能覆盖 Excel 中的“吉贵（原中意）玩具厂”。
- `外箱规格(CM)` 解析为 `carton_size_cm`，表示外箱尺寸，不是重量。
- `毛/净重(KG)` 可解析 `23/21` 或 `毛重23KG 净重21KG`，分别写入 `gross_weight_kg` / `net_weight_kg`，同时保留 `weight_text`。
- `内盒` / `内盒规格` 写入 `inner_box_quantity` / `inner_box_size_cm`。
- `棋盘规格` 如果是 `A*B*C` 维度格式，报价导入时可作为 `product_size_cm`；如果是文字规格，保留为 `product_spec` 和 `dimension_text`。

验证命令：

```bat
.\.venv\Scripts\python.exe -m unittest tests.test_excel_preview_tool tests.test_factory_quote_tool
.\.venv\Scripts\python.exe -m unittest tests.test_excel_preview_tool tests.test_factory_quote_tool tests.test_agent_loop tests.test_skill_registry tests.test_chat_ui_runtime
.\.venv\Scripts\python.exe -m py_compile tools\excel_table_classifier.py tools\factory_quote_tool.py tests\test_excel_preview_tool.py tests\test_factory_quote_tool.py
git diff --check
```

结果：

```text
Ran 31 tests in 1.143s
OK

Ran 62 tests in 1.413s
OK

py_compile 通过。
git diff --check 只有 Windows LF/CRLF 换行提示，没有空白错误。
```

交接注意：

- 当前分类器只做表头级字段识别和候选打分，不应在技能里直接生成最终用户 Markdown，最终展示仍交给 `core/ui_formatter.py`。
- 后续如果要让用户在 UI 里调整错误映射，需要把 `change_proposals` / `proposal` 接到确认弹窗，再把用户确认写入 `tools/excel_feedback_store.py`。
- 未来可把通用 Excel 入库拆成独立业务资料库，避免继续把所有归档行为塞进 `factory_quote_tool.py`。

## 2026-06-09 Excel 已确认映射归档写库修复

用户在确认最新 Excel 表格字段映射后要求写入数据库，执行失败：

```text
unsupported record_type: excel_table
```

根因：

- `official.business_records` 的内置 schema 只有 `customer/product/order/quote/inventory/purchase/finance/logistics/general` 等类型，没有 `excel_table`。
- 旧的 `D:\YoubestarLocal\data\business_records.sqlite3` 还可能缺少新版 `business_records` 表字段，例如 `content`，导致全库查询时报 `no such column: content`。

修复：

- `core/business_records_db.py` 新增内置资料类型 `excel_table`，用于“已确认字段映射后的通用 Excel 表格归档记录”。
- `excel_table` 关键字段包括：
  - `file_name`
  - `source_path`
  - `sheet_name`
  - `category`
  - `category_label`
  - `row_count`
  - `column_count`
  - `mapping_status`
  - `field_mappings`
  - `headers`
  - `rows`
  - `confirmed_by`
- `tools/business_records_tool.py` 支持从扁平参数中收集这些 Excel 归档字段，避免智能体或前端没有包 `fields` 时丢字段。
- `BusinessRecordsDB.ensure_ready()` 现在会补齐旧 SQLite 表缺失的所有核心列，包括 `content`、`tags_json`、`source`、`data_json`、`fields_json`、`search_text`、`created_by`、`updated_by`、`source_ip`、`created_at`、`updated_at`。
- 关键词查询加入 `business_key`，旧记录只有业务键时也能被搜到。
- `tools/factory_quote_tool.py` 补充中意/吉贵真实表头别名：`包装/品名`、`厂价(RMB)`、`数量(只)`，并让表头检测支持第 4 行这种非固定位置表头。

真实库查询结果：

- 报价专用库 `D:\YoubestarLocal\data\factory_quotes.sqlite3` 当前状态：工厂数 1、品牌数 0、产品数 121、最近导入 `2026-06-08T17:19:25.752872+00:00`。
- 在报价专用库中查询 `中意`：未找到。
- 在报价专用库中查询 `吉贵`：未找到。
- 在通用业务资料库中查询 `中意`：匹配数量 0。
- 在通用业务资料库中查询 `吉贵`：匹配数量 0。

后续真实写库结果：

- 已将 `D:\工厂资料\中意（吉贵）棋报价总1.xlsx` 的 `Sheet1` 写入通用业务资料库，类型为 `excel_table`，业务键为 `excel:f3064fc183d9`。
- 已将同一 Excel 导入报价专用库 `D:\YoubestarLocal\data\factory_quotes.sqlite3`。
- 导入摘要：工厂 `吉贵（原中意）玩具厂`，产品数 267，状态为“已写入本地报价资料库，未写入长期记忆”。
- 导入后报价专用库状态：工厂数 2、品牌数 0、产品数 388、最近导入 `2026-06-09T10:49:17.012909+00:00`。
- 查询 `中意` 和 `吉贵` 均可命中 `吉贵（原中意）玩具厂`；默认查询限制最多返回 200 条，实际本次导入产品数为 267。

验证命令：

```bat
.\.venv\Scripts\python.exe -m unittest tests.test_business_records_tool
.\.venv\Scripts\python.exe -m unittest tests.test_business_records_tool tests.test_server_chat_runtime tests.test_chat_ui_runtime tests.test_excel_preview_tool
.\.venv\Scripts\python.exe -m py_compile core\business_records_db.py tools\business_records_tool.py tests\test_business_records_tool.py
.\.venv\Scripts\python.exe -m unittest tests.test_factory_quote_tool tests.test_business_records_tool tests.test_excel_preview_tool tests.test_server_chat_runtime tests.test_chat_ui_runtime
```

结果：

```text
Ran 9 tests in 0.589s
OK

Ran 42 tests in 1.702s
OK

py_compile 通过。

Ran 63 tests in 2.153s
OK
```

## 已知注意事项

- 2026-06-07 已将模型聊天请求超时从 60 秒放宽到 120 秒，配置在 `core/llm.py` 的 `MODEL_RESPONSE_TIMEOUT_SECONDS`。
- `youbestar.json` 是本地配置，不能上传 GitHub。
- 官方技能文件当前是包装模块，实际逻辑仍复用 `tools/`。
- community skill 安装还没有做导入包流程，目前只预留了目录和命名空间。
- local skill 审批通过后会注册为 `local.*`。
- 前端技能页现在只做管理和展示，不做 marketplace。

## 建议下一步

优先级从高到低：

1. 先把 `official.web_query` 拆成 provider adapter，并增加新闻源适配。
2. 增加“搜索结果详情页阅读”能力，让 Agent 从摘要综合升级为正文综合。
3. 将搜索重试策略抽象进 `AgentPolicy`，保留 `MAX_SEARCH_ROUND=2 / MAX_TOOL_CALL=10 / MAX_RUNTIME=25秒` 硬限制。
4. 把理解/查询改写/综合/自检 prompt 从 `core/agent_nodes.py` 拆到独立配置或 prompt 模块。
5. 抽象 `AgentPolicy`，把是否综合、如何综合、是否继续追问、是否自检等策略从节点函数里拆出来。
6. 给 `intent`、`search_assessment`、`answer_check` 增加前端调试展示。
7. 接入记忆候选识别节点和前端确认弹窗，完成长期业务记忆确认闭环。
8. 增加短期实体记忆，支持“他/这个/上面那个”的指代解析。
9. 给 `synthesize` 节点补端到端测试，确保搜索成功时不会再显示执行失败或原始 dict。
10. 做 Skill Package 导入接口：读取 `meta.json + skill.py`。
11. 做 community skill 安装/卸载。
12. 支持同一技能多版本并切换默认版本。
13. 增加技能测试报告 UI。
14. 增加更多工具技能：Excel、OCR、网页抓取。
